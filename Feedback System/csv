import pandas as pd
import mysql.connector

conn = mysql.connector.connect(
    host="localhost",
    user="root",
    password="",
    database="your_db"
)

query = "SELECT * FROM feedback"

df = pd.read_sql(query, conn)

df.to_csv("data_lake/feedback_raw.csv", index=False)

print("Data exported to Data Lake")